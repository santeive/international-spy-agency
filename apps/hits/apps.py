"""
Hit app config
"""
from django.apps import AppConfig


class HitsConfig(AppConfig):
    """
    Hit app config class
    """
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.hits'
